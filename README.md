# Issue Time Tracker - Frontend

A React + TypeScript + Vite project with Tailwind CSS.

## Getting Started

```bash
pnpm install
pnpm dev
```

Dev server runs at [http://localhost:3000](http://localhost:3000).
